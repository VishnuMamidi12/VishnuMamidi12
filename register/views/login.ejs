<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>LOGIN</title>
    
    <style>
        * {
    -webkit-box-sizing: border-box;
    box-sizing: border-box;
  }

 body{
    margin: 0;
    background: #f4fef4;
    font:normal 14px/20px 'PT Sans', sans-serif;
    color:#000;
   }
   ul {
       list-style: none;
   }

   article,aside,details,figcaption,figure,
   footer,header,hgroup,menu { 
    display:block;
   }
   .container{
       padding: 0 15px;
        width: 100%;
   }
   .form-control {
       margin: 50px auto;
       width: 50%;
   }
   .form-control div {
       background: #fff;
       padding: 20px;
   }

   .form-control h1 {
       font-size: 25px;
       padding: 0 0 10px 0;
       margin-bottom: 30px;
       border-bottom: 2px solid #000;
   }
   .form-control label {
       display: block;
       padding:  0 0 5px;
       font-size: 12px;
       color: #777;
   }

   input {
       border: 1px solid #777;
       padding: 0 10px;
       font-size: 14px;
       color: #000;
       line-height: 45px;
       width: 320px;
   }

   .form-control li {
       margin-bottom: 15px;
   }

   button {
       background: #343434;
       color: #fff;
       font-size: 16px;
       line-height: 47px;
       border: none;
       padding: 0 30px;
       cursor: pointer;
       margin-right: 10px;
   }
   </style>
</head>
<body>
    <form action="/users/login" method="POST">
         <div class="form-control">
             <h1>Log In</h1>
                <ul>
                    <% if(messages.success_msg) { %>
                    <li><%= messages.success_msg %></li>
                    <% } %>
                </ul>
                <ul>
                    <% if (messages.error) { %>
                    <li><%= messages.error %></li>
                    <% } %>
                </ul>
             <ul>
                <li>
               <label>Email Address</label>
               <input type="email" id="email" name="email" placeholder="Enter Your Valid Email" required autocomplete="off">
                </li>
                <li>
               <label>Password</label>
               <input type="password" id="password" name="password" placeholder="Enter Password"></u>
               <li>
               <input type="submit" class="button" value="Login"> </li>
              <li> New around here?<a href="/users/register"> Register now</a>
               </li>
             </ul>
             
            </div>
        </form>
</body>
</html>